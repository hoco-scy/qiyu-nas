# Security policy

栖屿 NAS 面向个人自托管环境。请不要在公开 Issue 中提交密码、会话密钥、私有地址、日志中可能包含的文件名或其他敏感信息。

发现安全问题时，请通过 GitHub 的私密安全报告功能联系维护者，并尽量提供受影响版本、复现步骤和影响说明。

部署者应自行负责：使用强密码、妥善保存 `.env`、限制入口网络，并在通过 HTTPS 提供服务时将 `PORTAL_COOKIE_SECURE=true`。
